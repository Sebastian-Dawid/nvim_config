require("toggleterm").setup({
    size = 40,
    open_mapping = [[<C-\>]],
    hide_numbers = true,
    shade_terminals = true,
    shading_factor = 0.6,
    direction = 'float',
    shell = 'zsh',
})
